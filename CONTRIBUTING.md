# Contributing

## Setup

```bash
nvm use
cd mobile
npm ci
npm run typecheck
```

## iOS Development

```bash
cd mobile
npm run ios
```

Generated folders such as `mobile/node_modules/`, `mobile/.expo/`, and
`mobile/ios/` are intentionally ignored. Commit source, configuration, docs, and
the npm lockfile.
